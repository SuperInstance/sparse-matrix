[package]
name = "sparse-matrix"
version = "0.1.0"
description = "A Rust library for Sparse Matrix"
license = "MIT OR Apache-2.0"
repository = "https://github.com/casey-digennaro/sparse-matrix"
edition = "2021"

[dependencies]
